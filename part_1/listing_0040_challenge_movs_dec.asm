bits 16

mov ax, [bx + di - 37]
mov [si - 300], cx
mov dx, [bx - 32]
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
mov bp, [5]
mov bx, [3458]
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
