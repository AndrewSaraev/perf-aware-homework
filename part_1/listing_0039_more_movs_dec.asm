bits 16

mov si, bx
mov dh, al
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unknown op code
; unsupported mov op (mod other than 11)
; unsupported mov op (mod other than 11)
; unsupported mov op (mod other than 11)
; unknown op code
; unsupported mov op (mod other than 11)
; unknown op code
; unsupported mov op (mod other than 11)
; unknown op code
; unknown op code
; unsupported mov op (mod other than 11)
; unsupported mov op (mod other than 11)
; unsupported mov op (mod other than 11)
; unknown op code
